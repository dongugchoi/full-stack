리액트
자바스크립트 라이브러리
nodejs가 있어야 npm(Node Package Manager)을 이용해서 설치가 가능하다.

nodejs의 경우 js코드를 브라우저 밖에서 실행할 수 있게 해주는 런타임 환경
js를 콘솔 or 터미널에서 확인하는 것이 가능해짐으로써 서버로 만드는게 가능해졌다.

npm을 이용해서 리액트 프로젝트 생성하기

워크스페이스로 만들 폴더를 지정
터미널로 폴더 경로를 잘 잡는다.

npm init
패키지이름, 버전정보, 패키지 설명 등등 여러 정보를 정해줘야 한다.
package.json에 객체형태로 저장된다.

스프링부트는 디펜던시스에서 버전만 적어놓고 적용하면 바로 바뀐다.

npm install react

npx를 이용한 리액트 프로젝트 구성하기

워크스페이스로 사용할 폴더 만들기
터미널로 폴더의 경로를 잡는다.

터미널과 친해지기 -> 많이 써봐야함

npx를 이용해 리액트 앱 만들기
npx create-react todo-react-app;

App.js -> HTML을 JSX형태로 export

index.js -> app.js에서 만든 컴포넌트를 import해서 사용

index.html -> 컴포넌트한 코드들이 index.html에 보인다.

리액트 프론트엔드 서버 올리기
npm start	app.js
↓		↓
요청		index.js
↓		↓
localhost:3000	index.html

브라우저에서 화면이 어떻게 렌더링이 되는가
주소  -> 요청 -> 프론트엔드 서버
           화면   ←  index.html  ←

1.파싱 - html DOM Tree
           CSSOM Tree

두개를 합쳐 rendertree


SPA
한번 웹페이지를 로딩하면 유저가 새로고침을 하지 않는 이상 페이지를 새로 로딩하지 않는다.

CSR
자바스크립트가 동적으로 HTML을 재구성해 만드는 클라이언트

프론트엔드 서비스 개발
학습내용
컴포넌트
Hook ->리액트의 정수
Props
이벤트 핸들러

실습내용
App/Todo/AppTodo 컴포넌트 개발
Material-ui를 이용한 ui 개발
핸들러를 이용한 Todo 기능 개발

Props와 Hook
Props  = 부모컴포넌트 -> 자식컴포넌트
			읽기가능
Props : 부모 컴포넌트가 자식 컴포넌트에게 전달하는 값, props는 개체이고 뭔가를
넘겨받을 때 자식 컴포넌트에서는 props.이름으로 접근할 수 있다.
단방향 자식은 렌더링만가능

Hook
리액트 16.8에 새로 도입된 기능으로 함수형 컴포넌트에서 사용되는 몇가지 기술

Todo 컴포넌트를 하나 더 추가하고 item을 하나 더 만들어서 넘기기
id : "1"
title : "Hello World2"
done : false

Todo를 두 개 연속으로 늘어놓는 대신, 배열과 반복문을 이용하자.